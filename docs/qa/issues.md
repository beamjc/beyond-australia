# QA issues

Severity: **High** = misleading or broken for many visitors · **Medium** = broken/misleading in a specific journey · **Low** = polish.
Status: OPEN · FIXED (retested) · NEEDS OWNER · NEEDS THAI REVIEW · BLOCKED.

"Mock-verified" = retested only against mocked Supabase fixtures, not live content.

## Open — decisions or review needed

| ID | Sev | Area | Issue | Evidence | Status / recommendation |
|---|---|---|---|---|---|
| ISS-001 | High | Bilingual | Language switch only affects the shell (nav, hero, services, section headers, footer, CTAs, articles/events chrome). **All 11 tool components ignore it**: Budget Planner is Thai-only; Postcode Checker, Timeline, Top Universities, Study Options, Financial Calculator, Savings Calculator, Visa Strength and Visa Pathway are English-only; Checklist and FAQ show fixed mixed Thai/English content. | Code: none of these components call `useLanguage` (see progress.md inventory). Browser: EN mode shows Thai planner; TH mode shows English calculators. | NEEDS THAI REVIEW. Batch-by-batch: extract strings into `translations.ts` with EN+TH, one feature per batch (Batch 1 = Budget Planner). |
| ISS-003 | High | Budget Planner | Each pathway card shows "เปอร์เซนต์ที่วีซ่าจะผ่าน" with a figure like **93.6%** and a "Low Risk" badge. The figure is a sector grant rate × an uncited age multiplier (`CalculationEngine.ts:47–95, 207`), not the visitor's probability. Conflicts with CLAUDE.md (no approval-probability claims). | Screenshot `mobile-01-bachelor-default.png`; code. | NEEDS OWNER. Recommendation: remove the percentage from the card, or relabel as "average grant rate for this sector (Thai applicants, period X)" with source; keep scoring unchanged. Thai wording held in BSP-074/075. **How it is calculated (from code, 2026-09-24):** `min(98, max(8, sectorRate[sector][location] × ageMultiplier(sector, location, age)))`. Only sector (HE/VET), apply-from (Thailand/Australia) and age change it — not university tier, budget or IELTS, so all three Bachelor cards always show the same figure. Example: Bachelor, from Thailand, age 24 → 93.6 × 1.0 = 93.6 %; age 35 → 93.6 × 0.6 = 56.2 % "High Risk". Base rates are commented "Thai nationals, Jul 2025 – Mar 2026" and multipliers "calibrated against March 2026 statistics", but no source file/link exists in the repo (both present since the initial commit, 2026-08-08). The `trend` values are computed but never shown. Owner asked for the explanation; decision still open. |
| ISS-010 | Medium | Budget Planner | Short-course goal, age ≥ 31: WHM card correctly says "cannot apply", but the headline still uses **WHM** numbers and caption ("รวมค่าวีซ่า WAH…"), showing ฿133,132–฿259,924 and "enough money". | `desktop-06-short-age35.png`; code `BudgetStudyPlanner.tsx:113–123`. | NEEDS THAI REVIEW. Fix is ready to apply: use visitor-visa figures when WHM is unavailable, with the new caption BSP-053. Not applied because it needs a new Thai sentence. |
| ISS-011 | Low | Budget Planner | Age slider allows 15–17; WHM card shows as available below 18. | Code `BudgetStudyPlanner.tsx:32, :742`. | NEEDS OWNER + factual check FC-03. |
| ISS-012 | Low | Budget Planner | "ยังไม่สอบ" (never tested) is calculated like IELTS 5.0 but does not show the Level 1 advice shown for 4.5. | `CalculationEngine.ts:129`. | NEEDS OWNER (calculation/advice rule). |
| ISS-013 | Low | Budget Planner | Visa months shown with decimals ("~48.5 เดือน"). | `mobile-01` screenshot. | Question in BSP-063; round in code after editor answers. |
| ISS-015 | Medium | Build/config | `src/lib/supabase.ts` creates the client at import with `process.env…!`. Without the two env vars, **`next build` fails for every public page** (verified baseline, before any changes). | Baseline build log: "supabaseUrl is required" on `/`, `/articles`, `/events`. | OPEN — recommend a guard that disables content sections when env is missing. Not changed: touches deploy configuration; owner to confirm env vars are set in Cloudflare. |
| ISS-016 | Medium | Events/Articles | Fetch errors are swallowed: `/events` and `/articles` show the empty state ("No events yet") on a network/API error; home sections disappear silently. | Mock test "Supabase failure on home…"; code. | NEEDS THAI REVIEW (needs error + retry text). |
| ISS-017 | Low | Events/Articles | Dates are always English (`MMM d, yyyy`), even in Thai; event times use the viewer's browser timezone with no label. | Code `EventsSection.tsx:87`, `events/page.tsx`, article page. | NEEDS THAI REVIEW (format choice: Thai month names, Buddhist year?, "เวลาไทย"). |
| ISS-018 | Low | Articles | Unknown slug renders "Article not found." (English only) with HTTP **200** (soft 404). | Workers preview: `/articles/x` → 200; e2e test. | OPEN — Thai text needs review; 404 status needs server-side lookup (larger change). |
| ISS-019 | Medium | Postcode Checker | Result says "Postcode X is **eligible!**" — list membership is not full specified-work eligibility (industry, work dates, employer, and area rules apply). English-only. | Code `PostcodeChecker.tsx`. | NEEDS THAI REVIEW (EN+TH wording, e.g. "is in the specified-work postcode list for…"). |
| ISS-023 | Medium | Budget Planner | Taglines promise "งบไม่เกิน 100,000 บาท" (WHM) and "150,000 บาท" (visitor); the tool itself can compute ~฿452,700 for WHM. | BSP-109/113. | **Owner decision 2026-09-24: keep the promises.** Closed — no change. |
| ISS-024 | Low | Budget Planner | THB/AUD toggle buttons ≈ 24 px tall (small touch target). | Code `BudgetStudyPlanner.tsx:259`. | OPEN (low; bundle with the i18n change to avoid churn). |
| ISS-025 | Low | Contact links | LINE links use two formats: `line.me/ti/p/@beyondstudy` (CTA) and `line.me/R/ti/p/@beyondstudy` (planner). Both are valid LINE URL forms; destinations not changed. | Code. | Owner FYI — could not open them (network blocked). |
| ISS-027 | Medium | Savings Calculator | Tax labelled "2025–26" while today is in 2026–27; Medicare levy not modelled; minimum wage $24.95 likely stale. | FC-12…FC-14. | BLOCKED on official sources. |
| ISS-030 | Medium | Top Universities | World/Australia ranks shown without naming the ranking or year; 2026 fees not verifiable this session. Source URLs exist in data but are hidden. | FC-18. | **Owner decision 2026-09-24: keep as is;** owner will check when the rankings were retrieved. |
| ISS-031 | Low | Budget Planner | Visitor-visa card uses the WHM 17-week study cap. | FC-04. | BLOCKED on official source; calculation change needs owner. |
| ISS-032 | Low | Code | `VISA_FEE_WHM_AUD` comment says subclass 417; Thais use 462. | `CalculationEngine.ts:8`. | Trivial comment fix — left for the factual-check pass so fee + comment change together. |
| ISS-033 | Info | Security (out of scope) | Article HTML is rendered with `dangerouslySetInnerHTML`. Content is admin-authored (all accounts are admins by design), so risk is limited to admin accounts. | `articles/[slug]/page.tsx`. | Recorded only; admin scope excluded. |
| ISS-034 | Info | Checklist | No persistence and no reset button — ticks reset on reload. | Code. | NOT APPLICABLE per CLAUDE.md (test only if implemented). |
| ISS-036 | Low | Tabs a11y | New `role="tab"` buttons are reachable with Tab but do not implement Left/Right arrow-key movement from the ARIA tabs pattern. | Code `WHMSection.tsx`, `StudySection.tsx`. | OPEN — small follow-up (roving tabindex). |
| ISS-035 | Info | Home | Section named "reviews carousel" in the inventory is a static grid (no controls). | Code `ReviewsCarousel.tsx`. | Inventory correction only. |

## Fixed in this pass (non-copy; retested)

| ID | Sev | Area | Issue | Fix | Retest |
|---|---|---|---|---|---|
| ISS-002 | High | Home (all sections) | Scroll-linked fade made **visible** content translucent: at 390 px the end of the Study section (planner results + consultation CTA) was at **0.47 opacity** while on screen; `prefers-reduced-motion` was ignored (identical values). | `RevealOnScroll`: fade tied to the section's top edge only, no fade-out; CSS `motion-reduce:` override. | e2e "sections are fully opaque…" both motion settings × 3 viewports: PASS. Before: measured 0.47 / 0.80 / 0.78. |
| ISS-004 | High | Home | Template reviews ("[ชื่อผู้รีวิว 1]", "[วางข้อความรีวิวจริง…]") rendered publicly with 5 stars. | Entries whose name/text is still a `[...]` placeholder are not rendered; the block hides when none are real. Real reviews show automatically once `src/data/reviews.ts` is filled. | e2e "placeholder reviews are never rendered": PASS. **Owner: supply real, verbatim reviews.** Owner asked about pulling reviews from Google Maps (2026-09-24) — options in progress.md. |
| ISS-005 | Medium | WHM + Study tabs | Switching tabs unmounted the tool: Budget Planner, calculators, questionnaire progress were lost. | Panels stay mounted (`hidden`); proper `tablist/tab/tabpanel` + `aria-selected/controls`. | e2e "inputs survive switching…": PASS. |
| ISS-006 | Medium | Home events | "Upcoming Events" preview ordered ascending with `limit(3)` and no date filter → oldest (past) events shown. | Query adds `or(is_ongoing.eq.true, event_date.gte.<start of today>)`. `/events` page unchanged (lists all). | Mock-verified PASS. Live data BLOCKED. |
| ISS-007 | Medium | Financial Calculator | Typing `-3` children or a negative fee reduced the total (negative living/travel costs); child count > 10 accepted. | Clamp fee/paid ≥ 0, children 0–10, school-age 0–children. | e2e PASS ($31,710 and $99,006 derived by hand). |
| ISS-008 | Low | Budget Planner | THB⇄AUD toggle drifted the budget (฿800,000 → $34,072 → ฿800,011). | Budget stored once in AUD, displayed in selected currency. | e2e 3× round trip keeps ฿800,000: PASS. |
| ISS-009 | Medium | Article page | "Back to Articles" used `router.back()`: from a shared link it leaves the site / does nothing. Same on not-found. | Real link to `/articles`. | Mock-verified PASS. |
| ISS-014 | Medium | Articles | `tags` is nullable in the schema; `null` crashed list/card/detail (`.includes`, `.length`). | Null-safe `?? []`. | Mock fixture with `tags: null`: PASS, no page errors. |
| ISS-020 | Low | Study Options | Age accepted `-5`, `0`, `200`. | "Next" enabled only for whole numbers 15–70 (the input's own min/max). No new text. | e2e PASS. |
| ISS-021 | Medium | Accessibility | Sliders had no accessible name; labels not associated with inputs; choice buttons had no pressed state; expanders no `aria-expanded`; checklist ticks were unlabeled icon buttons (20 px); Visa Strength used `-400` text shades (low contrast on white) and coloured the *risky* end green on the two inverted factors. | Names from existing visible labels; `htmlFor/id`; `aria-pressed`/`aria-expanded`; checklist `role=checkbox` + larger hit area; `-700` shades; colours follow risk. | e2e keyboard slider tests (Home/End), label-based locators, checklist keyboard toggle: PASS. |
| ISS-026 | Low | Tooling | `npm run lint` was unconfigured (interactive prompt). | Added `.eslintrc.json` (`next/core-web-vitals`), disabled only `react/no-unescaped-entities` (6 pre-existing, cosmetic). | Lint: 0 errors, 5 `<img>` warnings (pre-existing). |


Owner decisions 2026-09-24: ⚠️ claims in BSP-001 (as-of date), BSP-040 ("Level 1"), BSP-062 ("No 485 pathway"), BSP-084/087 (50% upfront) — leave as they are.